// export const getBannerInfo = async () => {
//     const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/api/product/banner`);

//     return { status: res.status, data: await res.json() };
// };
